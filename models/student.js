class Student {
  constructor(
    studentId,
    firstName,
    lastName,
    gender,
    provinceName,
    registedYear,
    grade,
    statusId,
    majorId,
    degree,
  ) {
    this.student_id = studentId;
    this.firstname = firstName;
    this.lastname = lastName;
    this.gender = gender;
    this.province_name = provinceName;
    this.register_year = registedYear;
    this.grade = grade;
    this.status_id = statusId;
    this.magor_id = majorId;
    this.degree = degree;
  }
}
